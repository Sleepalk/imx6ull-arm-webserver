# webServer项目框架
大体思路（先只部署在Ubuntu）：服务端启动，开启线程池，使用epoll多路复用技术等待客户端连接，HTTP浏览器发送GET请求，访问服务端的 address + port + 请求资源，服务端收到数据包后，把它存到 接收缓冲区recvBuf中，根据主从状态机 分步骤解析缓冲区，把解析到的结果存储在每个变量中，供组装请求使用
主状态机： 请求首行，请求头，请求体
从状态机： 解析具体的一行

线程池部分：